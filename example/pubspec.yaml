name: example_for_oktoast
description: A new Flutter project.
version: 3.3.0+3
publish_to: none

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  oktoast:
    path: ../

dev_dependencies:
  flutter_lints: any

flutter:
  uses-material-design: true
